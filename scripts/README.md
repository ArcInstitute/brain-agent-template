# Scripts

The agent creates, modifies, and evolves all scripts in this directory.
These are the agent's own tools — it decides what to build and when.

Run scripts with: `uv run python scripts/<name>.py`
